package spil;

import gui.TUI;

import java.util.Scanner;

public class Controller {
    public static void main(String[] args){
        /*
        Die[] die = new Die[2];
        Player[] player = new Player[2];

        // Choose Language
        //...


        // Choose dice

        die[0] = new Die(TUI.getFace(1));
        die[1] = new Die(TUI.getFace(2));


        // Choose names

        player[0] = new Player(TUI.getName(1));
        player[1] = new Player(TUI.getName(2));


        //TUI.slag(score(rollSum));

        // Start spil

        do{
            Game.round(player[0], die[0]);
            game


        }while(!Game.isWinner(player1, player2));
        */
    }
}
